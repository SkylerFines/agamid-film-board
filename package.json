{
  "name": "film-board",
  "version": "0.1.0",
  "private": true,
  "description": "Shared film production dashboard. Node tooling is only needed for browser tests.",
  "scripts": {
    "test:browser": "playwright test",
    "check": "node --check web/app.js"
  },
  "devDependencies": {
    "@playwright/test": "1.63.0"
  }
}
